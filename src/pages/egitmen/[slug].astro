---
// src/pages/egitmen/[slug].astro
import Layout from '../../layouts/Layout.astro';
import { getAllInstructors } from '../../lib/instructors';

// ✅ getStaticPaths: Utility fonksiyonunu kullan
export async function getStaticPaths() {
  const egitmenler = getAllInstructors();
  
  return egitmenler.map((egitmen) => ({
    params: { slug: egitmen.slug },
    props: { egitmen },
  }));
}

// ✅ Props üzerinden eğitmeni al (Astro.params DEĞİL!)
const { egitmen } = Astro.props;

// Güvenlik kontrolü
if (!egitmen) {
  throw new Error('Eğitmen bulunamadı');
}

// Yardımcı değişkenler
const nameInitial = egitmen.name?.charAt(0).toUpperCase() ?? '?';

const specialtyColors = [
  'bg-orange-100 text-orange-700',
  'bg-blue-100 text-blue-700',
  'bg-green-100 text-green-700',
  'bg-purple-100 text-purple-700',
  'bg-pink-100 text-pink-700',
  'bg-indigo-100 text-indigo-700'
];
---

<Layout title={`${egitmen.name || 'Eğitmen'} | Eğitmen Profili | İstanbul Eğitim Merkezi`}>
  <div class="max-w-6xl mx-auto px-4 py-8">
    
    {/* HERO SECTION */}
    <div class="bg-gradient-to-br from-gray-900 to-gray-800 rounded-2xl shadow-lg p-6 md:p-8 mb-8">
      <div class="flex flex-col md:flex-row gap-8 items-start">
        <div class="flex-shrink-0">
          <div class="w-32 h-32 bg-gradient-to-br from-orange-400 to-amber-500 rounded-full flex items-center justify-center shadow-lg">
            <span class="text-5xl font-bold text-white">{nameInitial}</span>
          </div>
        </div>
        <div class="flex-1">
          <h1 class="text-3xl md:text-4xl font-bold text-white mb-3">{egitmen.name}</h1>
          <div class="flex flex-wrap items-center gap-3 mb-4">
            {egitmen.location && (
              <span class="bg-white/90 px-3 py-1 rounded-full text-sm text-gray-700">
                📍 {egitmen.location}
              </span>
            )}
            {egitmen.rating && (
              <span class="bg-yellow-100 px-3 py-1 rounded-full text-sm text-yellow-800">
                ⭐ {egitmen.rating}
              </span>
            )}
          </div>
          {egitmen.bio && <p class="text-gray-300 leading-relaxed">{egitmen.bio}</p>}
        </div>
      </div>
    </div>

    {/* UZMANLIK ALANLARI */}
    {egitmen.specialties?.length > 0 && (
      <div class="bg-blue-50 rounded-xl p-6 mb-8">
        <h2 class="text-xl font-bold text-gray-800 mb-3">⚡ Uzmanlık Alanları</h2>
        <div class="flex flex-wrap gap-2">
          {egitmen.specialties.map((specialty: string, index: number) => (
            <span 
              key={index}
              class={`px-4 py-2 rounded-full text-sm font-medium ${specialtyColors[index % specialtyColors.length]}`}
            >
              {specialty}
            </span>
          ))}
        </div>
      </div>
    )}

    {/* VERDİĞİ KURSLAR */}
    {egitmen.verdigiKurslar?.length > 0 && (
      <div class="mb-8">
        <h2 class="text-2xl font-bold text-gray-800 mb-4">📚 Verdiği Dersler</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
          {egitmen.verdigiKurslar.map((kurs: any) => {
            const getKategoriPrefix = (category: string) => {
              const cat = category?.toLowerCase() || '';
              if (cat.includes('bilişim') || cat.includes('yazılım')) return 'bilisim';
              if (cat.includes('dil')) return 'dil';
              if (cat.includes('spor')) return 'spor';
              if (cat.includes('sanat')) return 'sanat';
              if (cat.includes('lise')) return 'lise';
              if (cat.includes('ilköğretim')) return 'ilkogretim';
              return 'genel';
            };
            const prefix = getKategoriPrefix(kurs.category);
            const instructorInfo = kurs.instructorInfo;
            
            return (
              <a 
                href={`/kurs-${prefix}/${kurs.slug}`}
                class="block bg-white rounded-xl p-4 shadow hover:shadow-lg transition border border-gray-100"
              >
                <div class="flex items-start gap-3">
                  <img src={kurs.image} alt={kurs.title} class="w-16 h-16 rounded-lg object-cover flex-shrink-0" />
                  <div class="flex-1 min-w-0">
                    <h3 class="font-semibold text-gray-800 line-clamp-2 hover:text-indigo-600">{kurs.title}</h3>
                    <p class="text-xs text-gray-500 mt-1">{kurs.category}</p>
                    {instructorInfo?.privatePrice && (
                      <p class="text-orange-600 font-bold mt-2">{instructorInfo.privatePrice}₺</p>
                    )}
                  </div>
                </div>
              </a>
            );
          })}
        </div>
      </div>
    )}

    {/* İLETİŞİM BUTONLARI */}
    <div class="bg-gradient-to-r from-orange-500 to-amber-500 rounded-xl p-6 text-center">
      <h3 class="text-xl font-bold text-white mb-4">📞 Hemen İletişime Geçin</h3>
      <div class="flex flex-col sm:flex-row gap-3 justify-center">
        <a 
          href={`https://wa.me/905555555555?text=${encodeURIComponent(`${egitmen.name} hocamdan eğitim almak istiyorum.`)}`}
          target="_blank"
          rel="noopener noreferrer"
          class="bg-white text-orange-600 hover:bg-orange-50 px-6 py-3 rounded-full font-semibold transition"
        >
          💬 WhatsApp'tan Yaz
        </a>
        <a href="/derse-kaydol" class="bg-orange-700 text-white hover:bg-orange-800 px-6 py-3 rounded-full font-semibold transition">
          📝 Ders Talep Et
        </a>
      </div>
    </div>

  </div>
</Layout>